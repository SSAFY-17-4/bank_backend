bank:
    account:
        datasource:
            url: jdbc:mysql://${ACCOUNT_DB_HOST}:${ACCOUNT_DB_PORT}/${ACCOUNT_DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
            username: ${ACCOUNT_DB_USER}
            password: ${ACCOUNT_DB_PASSWORD}
            driver-class-name: com.mysql.cj.jdbc.Driver
